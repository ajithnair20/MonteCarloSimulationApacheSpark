import org.scalatest.FunSuite


class DataClientTest extends FunSuite {

  //Test cases to check functionality of API client
  /*test("API Client Fetches Data Correctly"){

    val client = new StockDataClient()
    val df = client.fetchData("MSFT")
    //test if Dataframe is fetched or not
    assert(df!=null)

    //test if DataFrame Loaded has more than one value and proper columns
    assert(df.columns.size == 6)

    //test if the DataFrame contains records
    assert(df.count() > 0)
  }*/
}
